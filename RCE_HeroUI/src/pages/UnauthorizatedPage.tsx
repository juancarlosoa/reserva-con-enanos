import DefaultLayout from "@/layouts/default";

export default function UnauthorizatedPage() {
    return (
        <DefaultLayout>
            <p>Unauthorizated Page</p>
        </DefaultLayout>
    );
}